[package]
name = "demo-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
btreemap-macro-rules = { path = "../btreemap-macro-rules" }
btreemap-proc = { path = "../btreemap-proc" }